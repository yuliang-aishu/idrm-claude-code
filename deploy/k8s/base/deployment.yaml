apiVersion: apps/v1
kind: Deployment
metadata:
  name: spec-cc-0104-api
  labels:
    app: spec-cc-0104
    component: api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: spec-cc-0104
      component: api
  template:
    metadata:
      labels:
        app: spec-cc-0104
        component: api
    spec:
      containers:
        - name: api
          image: docker.io/spec-cc-0104:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8888
              protocol: TCP
          envFrom:
            # 从 ConfigMap 加载非敏感配置
            - configMapRef:
                name: spec-cc-0104-config
            # 从 Secret 加载敏感配置
            - secretRef:
                name: spec-cc-0104-secret
          env:
            - name: TZ
              value: "Asia/Shanghai"
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8888
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8888
            initialDelaySeconds: 15
            periodSeconds: 20
